<h1>Profile</h1>
<br>
<table>
  <tr>
    <th>Email</th>
    <th>:</th>
    <td><%= current_user.email %></td>
  </tr>
  <tr>
    <th>Reviews</th>
    <th>:</th>
    <td><%= (current_user.reviews).count %></td>
  </tr>
  <tr>
    <th>Ratings</th>
    <th>:</th>
    <td><%= (current_user.ratings).count %></td>
  </tr>
</table>
<br>
<%= link_to 'Edit user', edit_user_registration_path(current_user), class: "btn btn-info" %>
<%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "btn btn-primary" %>
<br>
<br>
<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path('user'), data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger" %></p>

